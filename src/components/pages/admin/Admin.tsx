import React from 'react';

import UserList from './UserList';

const Admin = () => {
    const onCreate = () => {
        // TODO
    };

    const onEdit = () => {
        // TODO
    };

    return (
        <UserList
            onCreate={onCreate}
            onEdit={onEdit}
        />
    );
};

export default Admin;
